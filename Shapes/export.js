
const { handleCommand, handleMessages, cmd, commands } = require('./Shapes');
const { loadPlugins } = require('./plugins');


module.exports = {
    handleCommand,     
    handleMessages,    
    loadPlugins,      
    cmd,              
    commands          
};
